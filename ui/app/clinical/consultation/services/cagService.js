'use strict';

/**
 * 1.Ensure the patient belongs to a cag
 * 2. Get all patient list to assign the same observation assigned to the current patient e.g FollowUp date
 * 3. Save the information to al respective patients.
 *          * Use the API calls on cag service
 */
angular.module('bahmni.clinical')
    .service('cagService', ['$visitService','$http','appService','$q', function ($visitService,$http,appService,$q) {            
        
        var CagConceptSetPageController = '';

    }]);
